# form-fullstack

clone the repository

cd form-back

to install packages
npm i or npm i --force

to start the server
npm start


cd form-ui

to install packages
npm i or npm i --force


to start the server
npm run dev



